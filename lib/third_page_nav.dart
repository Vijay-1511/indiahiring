import 'package:flutter/material.dart';
class Third_item extends StatelessWidget {
  const Third_item({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(


      body: Center(
        child: Text("Page Third"),
      ),
    );
  }
}
